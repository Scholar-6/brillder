import { Brick } from "../../../../model/brick";
import { ProposalStep } from '../model';

export function validateProposal(brick: Brick) {
  let step = ProposalStep.BrickLength;
  if (!brick.title) {
  }
}